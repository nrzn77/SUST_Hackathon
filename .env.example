# Gemini reasoning is OPTIONAL. With no key, the service runs fully on the
# deterministic rule engine (all sample cases still pass).
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Optional overrides:
GEMINI_MODEL=gemini-2.0-flash
LLM_TIMEOUT_SECONDS=6
